program MJProgram 

int x;
char x2[];
const bool y2 = true;
enum nums {ONE, TWO, THREE=3}

{
	void m2(int x5[]) { 
	
	var.x.d[-5*8*9*8]++ ;
	
	var.a = varA * varB() * varC(false);
	var.b++ ;
	var.c();
	var.d = new klasa;
	var.e = (-25*9);
	
	break;
	continue;
	return;
	return 1-2;
	read(a);
	print('v');
	{ print('a'); }

	if (a < 6) break; 
	
	if (5 == f) read(a); else read(f);

	 }
	 
	 
}

